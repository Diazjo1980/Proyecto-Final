_schema-version: '3.2'
ID: PoryectoFinal
version: 0.0.1
modules:
  - name: poryectofinal-approuter
    type: approuter.nodejs
    path: poryectofinal-approuter
    requires:
      - name: PoryectoFinal_html_repo_runtime
      - name: uaa_PoryectoFinal
      - name: PoryectoFinal-dest-srv
        group: destinations
        properties:
          forwardAuthToken: false
          name: ui5
          url: 'https://ui5.sap.com'
    parameters:
      disk-quota: 256M
      memory: 256M
  - name: PoryectoFinal-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: PoryectoFinal-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - logaligroupfinalproject.zip
          name: logaligroupfinalproject
          target-path: resources/
  - name: logaligroupfinalproject
    type: html5
    path: finalproject
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - 'npm run build:cf'
      supported-platforms: []
resources:
  - name: PoryectoFinal_html_repo_runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
  - name: uaa_PoryectoFinal
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-name: PoryectoFinal-xsuaa-service
      service-plan: application
  - name: PoryectoFinal-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: PoryectoFinal-html5-srv
      service-plan: app-host
  - name: PoryectoFinal-dest-srv
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: 'https://ui5.sap.com'
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: PoryectoFinal-dest-srv
      service-plan: lite
parameters:
  deploy_mode: html5-repo
