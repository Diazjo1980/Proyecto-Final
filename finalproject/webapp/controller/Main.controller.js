sap.ui.define([
    "sap/ui/core/mvc/Controller"
],
    /**
     * @param {typeof sap.ui.core.mvc.Controller} Controller
     */
    function (Controller) {
        "use strict";

        function onInit () {
            
        };

        let Main = Controller.extend("logaligroup.finalproject.controller.Main", {

        });

        Main.prototype.onInit = onInit;

        return Main;


    });
